import 'package:flutter/material.dart';

class Earningstab extends StatelessWidget {
  const Earningstab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Hello Earnings'));
  }
}
